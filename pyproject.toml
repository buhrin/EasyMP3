[project]
name = "EasyMP3"
version = "1.1.0"
description = "Download MP3 audio with cropped cover art from the desktop or Chrome."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pyperclip>=1.11.0",
    "sv-ttk>=2.6.1",
]

[dependency-groups]
dev = ["pyinstaller>=6.22.2"]
